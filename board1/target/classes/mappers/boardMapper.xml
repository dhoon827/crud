<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.mappers.board">

	<!-- 게시물 목록 -->
	<select id="list" resultType="com.board.domain.BoardVO">
		select
		bnumber, bwriter, btitle,
		bcontents, bdate ,bhits
		from board.board2
		order by bnumber desc
	</select>
	<!-- 게시물 작성 -->
	<insert id="write" parameterType="com.board.domain.BoardVO">
		insert into
		board2(bnumber,
		bwriter, btitle, bcontents)
		values(SEQ_BOARD2.nextval, #{bwriter},
		#{btitle},
		#{bcontents})
	</insert>

	<!-- 게시물 조회 -->
	<select id="view" parameterType="int"
		resultType="com.board.domain.BoardVO">
		select
		bnumber, bwriter, btitle, bcontents, bdate, bhits
		from board2
		where
		bnumber = #{bnumber}
	</select>

	<!-- 게시물 수정 -->
	<update id="modify" parameterType="com.board.domain.BoardVO">
		update board2
		set
		btitle =
		#{btitle},
		bcontents = #{bcontents},
		bwriter = #{bwriter}
		where bnumber = #{bnumber}
	</update>

	<!-- 게시물 삭제 -->
	<delete id="delete" parameterType="int">
		delete
		from board2
		where bnumber = #{bnumber}
	</delete>
</mapper>